# قواعد وشروط المشروع - سوق مزاد

> هذا الملف يعمل كـ PROJECT_MASTER_GUIDE للمشروع، ويجب اعتباره المصدر الرئيسي للقواعد.

## القواعد الصارمة (لا استثناءات)

### 1. التصميم والواجهات
- **ممنوع نهائياً:** استخدام الإيموجي في الكود أو التصميم
- **مطلوب:** استخدام الأيقونات الاحترافية فقط من مكتبة:
-  - Heroicons
- **قاعدة:** عدم تغيير التصميم أو الشكل إلا بأمر صريح
- عند تصميم أي واجهة أو كود يتم استخدام الأيقونات الاحترافية مباشرة بدلاً من الإيموجي

### 2. اللغة والتواصل
- **لغة المحادثات:** العربية دائماً
- **لغة المشروع:** العربية
- **المنطقة:** ليبيا
- **العملة:** دينار ليبي (دينار)
- **الأرقام:** الأرقام العادية فقط (123) - لا أرقام هندية (١٢٣)

### 3. التقنيات المستخدمة (لا تغيير)
#### قواعد البيانات:
- **المستخدم:** PostgreSQL + Prisma
- **الممنوع:** SQLite, MySQL, MongoDB

#### التخزين المؤقت:
- **المستخدم:** KeyDB Alternative
- **الممنوع:** Redis

#### التقنيات الأساسية:
- React
- TypeScript
- Next.js
- TailwindCSS
- Monorepo Architecture

### 4. قواعد التطوير
- **لا تشغيل السيرفرات:** المستخدم يقوم بذلك يدوياً
- **الحفاظ على البنية:** عدم تغيير بنية المشروع الأساسية
- **استخدام المكونات الموجودة:** عدم إعادة اختراع العجلة
- **عدم كسر الوظائف:** التأكد من عمل كل شيء قبل التعديل

### 5. معلومات البيئة
- **قاعدة البيانات:** PostgreSQL على localhost:5432
- **KeyDB:** يعمل على localhost:6379
- **التطبيق:** يعمل على localhost:3021
- **Prisma:** متصل ومُعد بالكامل

## الأولويات عند أي عملية

1. **التوافق:** مع PostgreSQL و Prisma
2. **الاستقرار:** عدم كسر الوظائف الحالية
3. **الأداء:** استخدام المكونات المحسنة الموجودة
4. **الأمان:** اتباع معايير الأمان المطبقة
5. **اللغة:** كل النصوص بالعربية

## البنية الأساسية للمشروع

```
c:/sooq-mazad/
├── apps/
│   ├── admin/         # لوحة التحكم الإدارية (Next.js)
│   ├── api/           # سيرفر API داخلي
│   └── web/           # الموقع الرئيسي للمستخدمين
├── packages/
│   ├── config/        # إعدادات مشتركة
│   ├── database/      # تكامل قاعدة البيانات و Prisma
│   ├── types/         # الأنواع المشتركة TypeScript
│   └── ui/            # مكونات واجهة مستخدم مشتركة
├── database/          # سكربتات وإعدادات PostgreSQL
├── docs/              # مستندات تقنية للنظام
├── infrastructure/    # إعدادات البنية التحتية (CDN وغيرها)
└── apps/web/
    ├── lib/           # المكتبات والخدمات (Prisma, KeyDB, Auth...)
    ├── utils/         # الدوال المساعدة
    ├── components/    # مكونات React
    ├── pages/         # صفحات Next.js
    └── types/         # أنواع خاصة بتطبيق الويب

```

## المكونات الرئيسية المستخدمة

- **AdminLayoutWithTransitions:** تخطيط لوحة التحكم
- **SimpleAdminAuth:** نظام المصادقة الإداري
- **CheckboxField:** مكون موحد للاختيار
- **AdminTableAdapter:** جداول إدارية متجاوبة
- **TransportImageUploader:** رفع صور خدمات النقل
- **ComprehensivePhoneSystem:** نظام أرقام الهواتف الليبية

## ملاحظات مهمة

- المشروع يستخدم Turbo للـ build
- النظام يدعم أرقام الهواتف الليبية بالكامل
- جميع APIs محمية بنظام مصادقة
- النظام يدعم Soft Delete للبيانات الحساسة

## تعليمات إضافية من المالك (جلسة المراجعة الحالية)

- عدم تغيير تقنيات المشروع أو البنية الأساسية للمشروع نهائياً
- تم إلغاء Redis و SQLite بالكامل من المشروع واستبدال Redis بـ KeyDB
- عدم تشغيل أي سيرفر من داخل الأوامر الآلية، المالك يشغله يدوياً
- التواصل مع المالك دائماً بالعربية في الشات
- استخدام الأيقونات الاحترافية فقط من Heroicons وعدم استخدام الإيموجي في الكود أو التصميم
- الالتزام باستخدام الأرقام العادية فقط 123 وعدم استخدام الأرقام الهندية
- عند إجراء أي إصلاحات، يمنع تغيير التصميم أو الشكل إلا بأمر صريح من المالك
- تفضيل إعداد بيئة VS Code بإضافات مناسبة مثل Prisma و PostgreSQL و ESLint و TailwindCSS بما يتوافق مع هذا المشروع

---
**آخر تحديث:** 21 ديسمبر 2025
