# DateLocationInfo Component

مكون موحد لعرض معلومات التاريخ والموقع بطريقة متسقة ومرنة.

## 🎯 الهدف

- **منع التكرار**: تجنب كتابة نفس الكود في أماكن متعددة
- **الاتساق**: تصميم موحد في جميع أنحاء التطبيق
- **المرونة**: خيارات متعددة للتخصيص
- **سهولة الصيانة**: تحديث واحد يؤثر على جميع الاستخدامات

## 📦 التثبيت

```tsx
import DateLocationInfo from '../components/DateLocationInfo';
```

## 🚀 الاستخدام الأساسي

```tsx
<DateLocationInfo date={new Date()} location="طرابلس" />
```

## الإعدادات الخصائص (Props)

| الخاصية        | النوع                            | الافتراضي   | الوصف               |
| -------------- | -------------------------------- | ----------- | ------------------- |
| `date`         | `Date \| string`                 | -           | التاريخ المراد عرضه |
| `location`     | `string`                         | -           | الموقع المراد عرضه  |
| `className`    | `string`                         | `''`        | فئات CSS إضافية     |
| `showDate`     | `boolean`                        | `true`      | إظهار التاريخ       |
| `showLocation` | `boolean`                        | `true`      | إظهار الموقع        |
| `dateFormat`   | `'short' \| 'long' \| 'numeric'` | `'numeric'` | تنسيق التاريخ       |
| `size`         | `'sm' \| 'md' \| 'lg'`           | `'md'`      | حجم المكون          |

## 📏 الأحجام المتاحة

### صغير (sm)

- للبطاقات الصغيرة والقوائم المضغوطة
- أيقونات 3x3، نص xs

### متوسط (md) - افتراضي

- للاستخدام العام
- أيقونات 4x4، نص sm

### كبير (lg)

- للعناوين والصفحات الرئيسية
- أيقونات 5x5، نص base

## 📅 تنسيقات التاريخ

### رقمي (numeric) - افتراضي

```
20/7/2025
```

### مختصر (short)

```
20 يوليو
```

### مفصل (long)

```
الأحد، 20 يوليو 2025
```

## 💡 أمثلة الاستخدام

### عرض التاريخ والموقع معاً

```tsx
<DateLocationInfo date={listing.createdAt} location={listing.city} size="md" />
```

### عرض التاريخ فقط

```tsx
<DateLocationInfo date={auction.endTime} showLocation={false} dateFormat="long" />
```

### عرض الموقع فقط

```tsx
<DateLocationInfo location={car.location} showDate={false} size="sm" />
```

### مع تخصيص CSS

```tsx
<DateLocationInfo date={new Date()} location="بنغازي" className="border-t pt-4 text-blue-600" />
```

## 🎨 التخصيص

يمكن تخصيص المكون عبر:

1. **فئات CSS**: إضافة فئات مخصصة عبر `className`
2. **الحجم**: اختيار الحجم المناسب للسياق
3. **التنسيق**: اختيار تنسيق التاريخ المناسب
4. **العرض**: إخفاء التاريخ أو الموقع حسب الحاجة

## التحديث الترحيل من الكود القديم

### قبل:

```tsx
<div className="flex items-center gap-4 text-sm text-gray-500">
  <div className="flex items-center gap-1">
    <ClockIcon className="h-4 w-4" />
    <span>{new Date().toLocaleDateString('ar-LY')}</span>
  </div>
  <div className="flex items-center gap-1">
    <MapPinIcon className="h-4 w-4" />
    <span>طرابلس</span>
  </div>
</div>
```

### بعد:

```tsx
<DateLocationInfo date={new Date()} location="طرابلس" />
```

## 🌍 الدعم الدولي

- يدعم الاتجاه من اليمين لليسار (RTL)
- يستخدم التقويم الليبي للتواريخ
- يتعامل مع النصوص العربية بشكل صحيح

## 🛠️ الصيانة

لتحديث التصميم أو إضافة ميزات جديدة، قم بتعديل الملف:

```
components/DateLocationInfo.tsx
```

سيتم تطبيق التغييرات على جميع الاستخدامات تلقائياً.

## 📱 التجاوب

المكون متجاوب بالكامل ويعمل على جميع أحجام الشاشات.

## تم بنجاح أفضل الممارسات

1. استخدم الحجم المناسب للسياق
2. اختر تنسيق التاريخ المناسب للمساحة المتاحة
3. استخدم `showDate={false}` أو `showLocation={false}` عند عدم الحاجة
4. أضف `className` للتخصيص الإضافي
5. تأكد من تمرير تاريخ صحيح لتجنب الأخطاء
