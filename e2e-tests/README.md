# 🤖 بوت الاختبار الشامل - Sooq Mazad E2E Tests

نظام اختبار آلي شامل يحاكي تصرفات المستخدم الحقيقي في جميع صفحات المشروع.

## 📁 هيكل الملفات

```
e2e-tests/
├── helpers/
│   ├── test-data.ts      # بيانات الاختبار (مستخدمين، سيارات، إلخ)
│   └── test-helpers.ts   # دوال مساعدة للاختبارات
├── 01-public-pages.spec.ts    # اختبارات الصفحات العامة
├── 02-authentication.spec.ts   # اختبارات المصادقة
├── 03-ads-auctions.spec.ts    # اختبارات الإعلانات والمزادات
├── 04-admin-dashboard.spec.ts  # اختبارات لوحة التحكم
├── 05-api-database.spec.ts    # اختبارات APIs وقاعدة البيانات
└── 06-full-flow.spec.ts       # اختبار التدفق الكامل
```

## 🚀 طرق التشغيل

### 1. تشغيل جميع الاختبارات

```bash
npm run test:e2e
```

### 2. تشغيل بوت الاختبار الشامل

```bash
npm run test:bot
```

### 3. تشغيل الاختبارات مع المتصفح المرئي

```bash
npm run test:e2e:headed
```

### 4. تشغيل الاختبارات في وضع التصحيح

```bash
npm run test:e2e:debug
```

### 5. عرض واجهة Playwright التفاعلية

```bash
npm run test:e2e:ui
```

### 6. عرض تقرير HTML

```bash
npm run test:e2e:report
```

## ⚠️ متطلبات التشغيل

### 1. تشغيل الخادم أولاً

```bash
npm run dev:web
```

الخادم يجب أن يعمل على `http://localhost:3021`

### 2. تثبيت متصفحات Playwright

```bash
npx playwright install
```

## 📋 ما يتم اختباره

### الصفحات العامة

- ✅ الصفحة الرئيسية
- ✅ السوق الفوري
- ✅ المزادات
- ✅ خدمات النقل
- ✅ تسجيل الدخول
- ✅ التسجيل

### المصادقة

- ✅ تسجيل مستخدم جديد
- ✅ تسجيل الدخول
- ✅ دخول المدير
- ✅ معالجة البيانات الخاطئة

### لوحة التحكم الإدارية

- ✅ جميع الصفحات الإدارية (13 صفحة)
- ✅ إدارة المستخدمين
- ✅ إدارة المزادات
- ✅ إدارة السوق
- ✅ إدارة خدمات النقل
- ✅ العمليات المجمعة
- ✅ البحث والفلترة

### APIs وقاعدة البيانات

- ✅ فحص APIs العامة
- ✅ فحص APIs الإدارية
- ✅ فحص اتصال قاعدة البيانات
- ✅ قياس أوقات الاستجابة
- ✅ فحص الأمان

### التدفق الكامل

- ✅ رحلة الزائر الكاملة
- ✅ رحلة المدير الكاملة
- ✅ اختبار الأداء
- ✅ اختبار الاستقرار

## 📊 التقارير

بعد تشغيل الاختبارات، ستجد التقارير في:

| النوع       | المسار                          |
| ----------- | ------------------------------- |
| HTML        | `test-reports/html/index.html`  |
| JSON        | `test-results/test-report.json` |
| Markdown    | `test-results/test-report.md`   |
| Screenshots | `test-results/screenshots/`     |

## 🔧 الإعدادات

### playwright.config.ts

- **المنفذ:** 3021
- **المتصفحات:** Chromium, Firefox, Mobile Chrome
- **اللغة:** العربية (ar-LY)
- **المنطقة الزمنية:** Africa/Tripoli
- **عدد المحاولات:** 1 (محلياً)، 2 (CI)

## 🎯 بيانات الاختبار

### المدير الافتراضي

```
المستخدم: admin
كلمة المرور: 123456
```

### مستخدم الاختبار

```
الهاتف: +218900000000
كلمة المرور: admin123
```

## 📱 دعم المتصفحات

| المتصفح       | الإصدار    |
| ------------- | ---------- |
| Chromium      | أحدث إصدار |
| Firefox       | أحدث إصدار |
| Mobile Chrome | Pixel 5    |

## 🔄 التكامل المستمر (CI)

لتشغيل الاختبارات في CI/CD:

```yaml
- name: Run E2E Tests
  run: npx playwright test
  env:
    CI: true
```

## 📝 إضافة اختبارات جديدة

### 1. إنشاء ملف اختبار

```typescript
import { test, expect } from '@playwright/test';
import { PAGES, TIMEOUTS } from './helpers/test-data';
import { navigateTo } from './helpers/test-helpers';

test.describe('اسم المجموعة', () => {
  test('اسم الاختبار', async ({ page }) => {
    await navigateTo(page, PAGES.home);
    // اكتب اختبارك هنا
  });
});
```

### 2. استخدام الدوال المساعدة

```typescript
import { loginAdmin, loginUser, fillForm } from './helpers/test-helpers';

// تسجيل دخول المدير
await loginAdmin(page);

// تسجيل دخول المستخدم
await loginUser(page, phone, password);

// ملء نموذج
await fillForm(page, { name: 'القيمة' });
```

## 🐛 استكشاف الأخطاء

### الاختبارات تفشل بسبب timeout

- تأكد من تشغيل الخادم على المنفذ 3021
- زد قيمة timeout في الإعدادات

### الاختبارات تفشل في CI

- تأكد من تثبيت المتصفحات: `npx playwright install --with-deps`
- استخدم `--headed=false`

### لا تظهر الصفحات بشكل صحيح

- تأكد من بناء المشروع: `npm run build`
- تأكد من تشغيل قاعدة البيانات

---

**تم إنشاء هذا النظام لاختبار مشروع سوق مزاد بشكل شامل وآلي.**
