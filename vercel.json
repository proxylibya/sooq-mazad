{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "ls -la && ls -R && npx prisma generate --schema=./prisma/schema.prisma && npx next build",
  "outputDirectory": ".next"
}
